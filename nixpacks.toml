# Nixpacks configuration for Railway - Python Telegram Bot
# Fix for "pip: command not found" error

providers = ["python"]

[start]
cmd = "python anonymous_chat_bot.py"

[variables]
PYTHONUNBUFFERED = "1"
NIXPACKS_PYTHON_VERSION = "3.11"
